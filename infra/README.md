# infra

This directory contains reusable Bicep modules and examples for the Platform Landing Zone.

- `modules/` — Bicep modules (hub, spoke, firewall, monitoring, roles, secrets)
- `examples/` — Parameter files and examples for non-production and production deployments
- `policy/` — Azure Policy initiatives and definitions

