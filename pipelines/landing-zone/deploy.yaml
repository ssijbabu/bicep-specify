trigger:
  branches:
    include: [ main ]

stages:
  - stage: Deploy
    displayName: 'Deploy Landing Zone'
    jobs:
      - deployment: DeployToNonProd
        displayName: 'Deploy to Non-Prod'
        environment: 'non-prod'
        pool:
          vmImage: 'ubuntu-latest'
        strategy:
          runOnce:
            deploy:
              steps:
                - script: |
                    echo 'This is a placeholder for deploy steps. The real pipeline should run ARM/Bicep deployment tasks with parameter files and approvals.'
                  displayName: 'Placeholder deploy step'
                - script: |
                    echo 'Manual approval required for production.'
                  displayName: 'Production approval notice'
